server:
  port: 0

spring:
  profiles:
    include: token
  jpa:
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console

telegram:
  bot:
    username: eomisae-notify-bot
    key: key


logging:
  level:
    org.hibernate.SQL: DEBUG